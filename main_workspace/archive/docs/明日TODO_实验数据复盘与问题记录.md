# 明日 TODO：实验数据复盘与潜在问题记录

目的：先通读中期报告、再逐项核对实验代码与已有数据，把「还没处理的雷」圈出来并记录，再继续实验。

---

## Part A：明日执行清单（Step by Step）

按顺序打勾，建议半天内完成 Part A 的 1–4，下午或次日做 5。

---

### Step 1：通读中期报告（约 30–45 分钟）

- [ ] **1.1** 读 **Abstract + 1 Introduction**：确认报告里对外的「已做 / 未做」表述，用便签或高亮标出：
  - 报告里写了「已完成」的内容；
  - 报告里写了「尚未」或「下一阶段」的内容。
- [ ] **1.2** 读 **2 Literature Review**：快速过，重点看 2.2 排卵生理、2.3 计算方法（日历 vs 生理），心里有数即可。
- [ ] **1.3** 读 **3 Methodology**（重点）：
  - 3.2 数据与特征：周期定义、排卵标注定义、**哪些做模型输入、哪些只做标注**、标准化方式；
  - 3.3 模型：主任务（经期预测）、辅助任务（排卵）、mask 方式；
  - 3.4 评估：subject-level 划分、指标。
- [ ] **1.4** 读 **4 Implementation and Preliminary Results**：
  - 对照 4.1.1–4.1.4，逐条看「报告里说做了啥」vs「你记得实际做了啥 / 有啥坑」；
  - 在报告空白处或单独纸写下：**和实际不一致 / 当时没写进去的问题**（例如：某步只做了一半、某数据有异常、某参数没写进报告等）。
- [ ] **1.5** 读 **5 Progress + Reflection**：看时间线与反思，回忆当时卡在哪、妥协了哪些点。

**输出**：报告上或单独一页的「报告 vs 实际」简记 + 回忆起的疑点列表（哪怕不完整也没关系，Step 4 会补）。

---

### Step 2：按流程过一遍 cycle_anchor 实验代码（约 45–60 分钟）

路径：`main_workspace/data_process/cycle_anchor.ipynb`。按 notebook 从上到下，不要跳。

- [X] **2.1** **加载与提取**（Section 1–3）：
  - 确认读的是 `hormones_and_selfreport.csv`，提取列是否只有 id, study_interval, day_in_study, phase, lh, estrogen；
  - 是否有用错文件或漏列。
- [X] **2.2** **周期识别与分组**（Section 4–5）：
  - 周期定义：是否「从 Menstrual 开始到下一个 Menstrual 之前」；
  - cycle 0 的处理：数据开头不是 Menstrual 时如何标；
  - 大组 = id + study_interval，小组 = cycle；
  - 记录：**当前代码里 cycle_id 是否被保存进最终 CSV？**（若没有，下游做序列模型要自己重新算或补写。）
- [X] **2.3** **缺失与完整周期**（Section 5–6）：
  - 完整周期 = 该周期内 lh/estrogen/phase 无缺失；
  - 是否剔除了「有缺失」的整周期；
  - 记录：**缺失判定是按「行」还是「周期内任一天缺就整周期删」？**
- [X] **2.4** **日期连续性与删除**（Section 6–7.1）：
  - 不连续判定：周期内 day_in_study 是否严格连续（无断天）；
  - 报告写「移除 18_2024 Cycle 1」；
  - **重点**：移除后是否**只**删了 Cycle 1？**18_2024 Cycle 2 是否仍在不连续名单里但被保留？** 若保留，则当前 109 个周期里仍有 **1 个周期内部缺天**（例如缺 day 974），序列模型按「连续天」假设会踩雷。
- [X] **2.5** **LH Surge 与排卵日标注**（Section 8–9）：
  - 基线：月经结束后 1–6 天 LH 均值、去噪（去最大最小）；无月经期时用周期前 6 天；
  - Surge：从基线后第 7 天起，LH/基线 ≥ 某阈值、连续天算一段；多段取最后一段；
  - **阈值**：代码里实际用的是 **2.0** 还是 **2.5**？注释/文档若写 2.5 而代码 2.0，记入「潜在问题」；
  - Method 1 = surge 第一天 +1，Method 2 = surge 峰值日 +1（多峰取第一个）；
  - 记录：**有多少个周期算出了 baseline？多少个检测到 surge？**（例如 103 baseline、81 surge → 28 个周期无排卵标注，辅助任务会非常稀疏。）
- [X] **2.6** **保存与导出**：
  - 最终写入 `cycle_anchor_clean.csv` 的列有哪些；
  - 是否包含 cycle_id、day_in_cycle、next_menses_start_day 等（若没有，后面做模型要补）。

**输出**：在「实验数据潜在问题清单」里填上 Step 2 发现的问题（见 Part B）。

---

### Step 3：核对已产出的数据文件（约 20–30 分钟）

- [X] **3.1** **cycle_anchor.csv**：
  - 行数、列名；
  - 是否包含全部 5659 行（或当时的原始行数）；
  - 缺失值：lh/estrogen 缺失比例是否和报告/文档一致。
- [X] **3.2** **cycle_anchor_clean.csv**：
  - 行数（应为 2796 + header）、列名；
  - 是否**只有** id, study_interval, day_in_study, phase, lh, estrogen, ovulation_day_method1, ovulation_day_method2；
  - 是否有 **cycle_id / day_in_cycle / next_menses_start_day**（若无，记入问题）；
  - 抽查 2–3 个 (id, study_interval)：同一周期内 day_in_study 是否连续（尤其 18_2024 的 cycle 2）；
  - ovulation_day_method1/2：有多少行 =1？是否约 78/77（若与 notebook 输出一致）；多少周期整周期全 0（无排卵标注）。
- [X] **3.3** 其他：若有别的已处理表（如中间表、汇总表），列名与用途是否清楚，是否和报告/数据处理计划一致。

**输出**：在问题清单里补充「缺失列」「不连续周期仍存在」「排卵标注稀疏」等。

---

### Step 4：整理「实验数据潜在问题清单」（Part B）

- [X] **4.1** 新建或打开 `main_workspace/docs/实验数据潜在问题清单.md`（见下方 Part B 模板）。
- [X] **4.2** 把 Step 1–3 发现的所有点填进去：
  - 问题简述；
  - 来源（报告/代码/数据）；
  - 是否必须修再继续 / 可延后；
  - 建议动作（修数据 / 修代码 / 文档注明）。
- [X] **4.3** 标出 **必须先修的 1–3 条**，作为「继续实验前必做」。

**输出**：一份可维护的「实验数据潜在问题清单」+ 优先修复项。

---

### Step 5：按优先级修 1–3 个必修项，再继续实验

- [ ] **5.1** 从问题清单里选 1–3 个「必须修再继续」的项（例如：不连续周期、缺失 cycle_id、阈值不一致）。
- [ ] **5.2** 逐项修：改代码或重跑 notebook、或导出时补列、或删掉 18_2024 Cycle 2 等，并更新 `cycle_anchor_clean.csv` 或说明文档。
- [ ] **5.3** 修完后在问题清单里把对应项标成「已处理」或「已记录」，再按 `数据处理计划_基于中期报告.md` 做后续可穿戴特征与建模。

---

## Part B：实验数据潜在问题清单（模板）

下面是你**明天复盘时**要往里面填的模板。已根据当前代码与数据预填了部分「可能存在的雷」，你核对后可以改、删、补。

| # | 问题简述                                                                                                                                                                                                                                 | 来源                     | 必须修再继续？ | 建议动作                                                                                | 状态     |
| - | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | -------------- | --------------------------------------------------------------------------------------- | -------- |
| 1 | **109 个周期中仍有 1 个不连续周期**：18_2024 Cycle 2 内部缺 day 974；当前只移除了 18_2024 Cycle 1，Cycle 2 仍留在 clean 中。序列模型若假设「周期内 day 连续」会出错。                                                              | 代码 7.1 输出 + 每周报告 | 建议是         | 要么从 clean 中再移除 18_2024 Cycle 2，要么在模型侧对「不连续周期」单独处理/标注        | 待你确认 |
| 2 | **cycle_anchor_clean.csv 无 cycle_id / day_in_cycle**：最终导出只含 id, study_interval, day_in_study, phase, lh, estrogen, ovulation_day_method1/2。下游做序列模型需要按周期分组，需自行从 phase 重算 cycle 或从 notebook 补导出。 | 代码保存逻辑 + CSV 列名  | 建议是         | 在 notebook 导出时增加 cycle_id（及可选 day_in_cycle）；或单独写脚本从 phase 推导并合并 | 待你确认 |
| 3 | **无 next_menses_start_day 或 days_until_next_menses**：主任务 time-to-event 需要「距下次经期天数」。当前 clean 表没有该列。                                                                                                       | 数据处理计划 vs 现有 CSV | 是             | 在周期表或日表中增加「该周期下次月经开始日」或直接算 days_until_next_menses             | 待你确认 |
| 4 | **排卵标注极稀疏**：109 周期中约 81 个有 surge，即约 28 个周期整周期无排卵日标注（全 0）；有标注的仅约 78/77 天为 1。辅助任务监督很稀疏，需在模型设计/评估时考虑。                                                                 | notebook 输出 + 报告 3.3 | 可延后         | 文档注明；评估时报告「有排卵标注的周期数/天数」；必要时做敏感性分析                     | 待你确认 |
| 5 | **Surge 阈值文档与代码不一致**：find_surge_segments 注释写「>= 2.5」，实际代码为 ratio >= 2.0；每周报告写 2.0。                                                                                                                    | notebook 注释 vs 代码    | 建议修         | 统一为 2.0 或 2.5，并改注释/文档保持一致                                                | 待你确认 |
| 6 | （你复盘时补充：报告里写了但实际没做的步骤）                                                                                                                                                                                             | 报告 vs 实际             | ？             | ？                                                                                      | 待填     |
| 7 | （你复盘时补充：当时记得有问题但没记下来的点）                                                                                                                                                                                           | 回忆 + 代码/数据         | ？             | ？                                                                                      | 待填     |

---

## Part C：复盘完成后「继续实验」的入口

- 数据处理计划文档：`main_workspace/docs/数据处理计划_基于中期报告.md`
- 下一步：完成 Part A 的 Step 5 后，从**阶段 1（可穿戴日级聚合）**开始做 HRV/WST/睡眠特征，并和 cycle_anchor_clean（或修后的版本）对齐。

---

**明天执行顺序建议**：Step 1 → Step 2 → Step 3 → Step 4（边做边填问题清单）→ 选 Step 5 的 1–3 项修完 → 再继续数据处理计划。
